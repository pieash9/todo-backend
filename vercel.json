{
  "version": 2,
  "builds": [
    {
      "src": "api/graphql.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/graphql",
      "methods": ["GET", "POST", "OPTIONS"],
      "dest": "/api/graphql"
    }
  ]
}
